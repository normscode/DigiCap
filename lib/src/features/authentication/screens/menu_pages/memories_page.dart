import 'package:flutter/material.dart';

class MemoriesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Manage Memories")),
      body: Center(child: Text("All your memories")),
    );
  }
}
